# Centralize Trusted Admin authority

Trusted Admin authority is resolved through one deep module from the Site Owner's Nostr document, with the last validated set used only as a stale availability fallback. The Site Owner is always trusted, legacy browser lists are never authoritative, revocation removes trust from past content, and appointment changes require fresh authority; this preserves one trust seam while keeping schedule and template publication in their own modules.
